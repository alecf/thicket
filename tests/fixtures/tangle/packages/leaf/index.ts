// One symbol back into alpha. Cutting either leaf edge detaches this package
// and leaves the alpha/beta/gamma ring exactly as tangled as it was.
import { alpha1 } from "../alpha/index.js";

export const leafOne = () => alpha1;
