// A checkout of the tree inside a dot-directory. Real repositories keep agent
// worktrees here, and counting them would double every file in the repo.
export function thing(value: number): number {
  return value * 2;
}
