export function usePricingTracking(input: {
  open: boolean;
  mode: string;
  onModeChange: string;
  livemode: boolean;
  source: string;
  variant: string;
}): void {
  void input;
}

export function useMatterTracking(input: {
  matterId: string;
  stage: string;
  ownerId: string;
  livemode: boolean;
  source: string;
  variant: string;
  channel: string;
}): void {
  void input;
}
